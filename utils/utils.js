//utils file will hold styling for similar styling needed

  //create style for container
  export const container = {
    flex: 1,
    padding: 32,
    marginTop: 50,
    backgroundColor: '#F6EADB'
  };

  //styling for intro text
  export const headerText = {
    fontSize: 20,
    marginBottom: 10,
    textAlign: 'center',
  };

  //styling for subtext
  export const subText = {
    fontSize: 15,
    marginBottom: 10,
    textAlign: 'center'
  };

  //styling text inside button
  export const buttonText = {
    color: '#fff',
    fontSize: 20,
    letterSpacing: 0.5,
  };